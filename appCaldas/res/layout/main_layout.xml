<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    >
	<LinearLayout android:id="@+id/contentViewLayout" 
		android:orientation="vertical"
		android:layout_width="fill_parent" 
		android:layout_height="fill_parent" 
		android:background="#EEE" android:layout_marginBottom="55dip"/>
	<HorizontalScrollView
    	android:id="@+id/bottomBar"
    	android:scrollbarTrackHorizontal="@drawable/ic_launcher"
    	android:scrollbarThumbHorizontal="@drawable/ic_launcher"
    	android:scrollbarSize="2dip"
    	android:layout_alignParentBottom="true" 
    	android:layout_width="fill_parent"
    	android:background="@drawable/ic_launcher" android:layout_height="55dip">
    	<RadioGroup
        		android:layout_width="fill_parent"
        		android:layout_height="wrap_content"
        		android:orientation="horizontal"
        		android:id="@+id/bottomMenu">
    	</RadioGroup>
	</HorizontalScrollView>
</RelativeLayout>
